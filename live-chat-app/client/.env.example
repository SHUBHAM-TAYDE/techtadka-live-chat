# Backend API base URL — leave blank when using Vite proxy in dev
# In production, set to your domain: https://yourdomain.com/api
VITE_API_URL=

# Socket.io server URL — leave blank when using Vite proxy in dev
# In production, set to your domain: https://yourdomain.com
VITE_SOCKET_URL=
